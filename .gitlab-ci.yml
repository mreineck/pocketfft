image: debian:testing-slim

stages:
  - testing

test:
  stage: testing
  script:
    - apt-get update
    - apt-get install -y gcc autotools-dev
    - autoreconf -i
    - CFLAGS="-O3" ./configure
    - make distcheck
